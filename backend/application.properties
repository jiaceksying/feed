# ============================================================
# 童心日记 — 后端配置
# 修改后重启服务生效；也可用命令行参数覆盖，如 --server.port=9090
# ============================================================

# ---- HTTP 服务 ----
server.host=0.0.0.0
server.port=8080

# 前端静态资源目录（相对于 backend 目录；默认托管项目根目录的前端页面）
frontend.dir=..

# ---- MySQL 数据库 ----
db.host=8.156.64.159
db.port=3306
db.name=feed
db.user=feed
db.password=feed1@qWaSzX
