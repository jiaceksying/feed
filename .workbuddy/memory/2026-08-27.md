- 2026-08-27 喝奶记录用量统一显示 ml 单位：新增 _formatMl()（纯数字自动补 ml，已含 ml/毫升 则原样），应用于时间线卡片、PDF 导出、CSV 导出；添加表单输入框旁加 ml 标识并在回显时去掉重复单位。
- 2026-08-27 新增数据库写入层：js/database.js（DB_CONFIG 预留连接配置，enabled=false、apiBaseUrl/token/direct 直连参数留空待填；写操作队列存 localStorage，配置好后自动 flush）；storage.js 改为双写（saveChild/addRecord/updateRecord/deleteRecord/clearAllRecords/importData 后入队同步）；设置页新增「数据库同步」卡片（状态徽章、待同步条数、测试连接/立即同步/全量推送按钮）；新增 database/schema.sql（MySQL 建表：child、records 含 milk_feeds/diaper_changes/solid_foods JSON 字段、sync_log，及后端 API 端点约定）。
